# Abhängigkeiten für den Hugging Face Space (CPU-Inferenz, headless).
# Beim Anlegen des Space als requirements.txt im Space-Root hochladen.
torch
gym==0.25.2                 # alte gym-API (passt zum Code, s. DEPLOY.md)
gym-super-mario-bros==7.4.0
nes-py==8.2.1
numpy<2                     # gym 0.25.2 verträgt kein NumPy 2.0
opencv-python-headless<4.11 # headless + NumPy<2-kompatibel
imageio
gradio
# PPO-Level (2-1/2-2/2-4): SB3-Stack zum Laden der Policies. Versionen wie im
# .venv-ppo gepinnt, damit die gespeicherten Modelle sicher laden.
stable-baselines3==2.3.2
gymnasium==0.29.1
shimmy==1.3.0
